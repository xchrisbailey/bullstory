import { Layout } from '../components/layout'

export const Home = (): JSX.Element => (
  <Layout>
    <p>
      Enter a ticker to see the days high low and a simple chart of its movement
      the last couple of months
    </p>
  </Layout>
)

export default Home
